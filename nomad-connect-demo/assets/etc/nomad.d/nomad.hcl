## Client and server configuration

log_level = "DEBUG"

## The data_dir is generally configured in the static files. Since
## this demo uses the current working directory, it's easier to use
## the command line flag instead.
data_dir  = "/opt/nomad/data"
